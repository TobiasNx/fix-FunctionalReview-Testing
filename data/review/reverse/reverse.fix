reverse("animal")
reverse("animols.type")

reverse("others[].1.name")

reverse("animals[]")

# These seem not to work perhapse due to (JSON-)Array. Either way does not work.
reverse("animals[].*")

# THis does not work. perhaps due to the `*`.
reverse("OTHERS[].*.name")